PORT=3000
DATABASE_PATH=./data/portfolio.db

# CORS settings
# Production: set explicit frontend origins (comma-separated)
# CORS_ORIGIN_ALLOWLIST=https://portfolio.example.com,https://www.portfolio.example.com
# Local dev: allow your frontend origin(s)
# CORS_ORIGIN_ALLOWLIST=http://localhost:3100
# Optional: allow any origin (handy for local dev only; not for production)
# CORS_ALLOW_ALL=1

# Postgres / Prisma.
# Required for Prisma CLI (migrate, generate). Production: use AWS Secrets Manager / RDS.
# For local SQLite-only dev, comment out DATABASE_URL (app uses DATABASE_PATH); then you cannot run prisma migrate.
DATABASE_URL=postgresql://portfolio:portfolio@localhost:5432/portfolio?schema=public

# Optional local development fallback if DATABASE_URL is not set.
# DB_HOST=
# DB_PORT=5432
# DB_USER=
# DB_PASSWORD=
# DB_NAME=portfolio

# Optional S3-backed file storage for uploads (project images) and resume.
# In production on ECS, set S3_BUCKET + AWS_REGION and give the task S3 read/write IAM permission.
# S3_BUCKET=portfolio-assets
# AWS_REGION=us-east-1
# Optional: public base URL for assets (e.g. https://api.example.com or CloudFront). When set, upload API returns full URLs.
# ASSETS_BASE_URL=https://api.example.com
